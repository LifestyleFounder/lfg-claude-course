{
  "_comment": "Fallback example file. The skill normally reads from ~/.lfg/call-pipeline.json (created by /lfg:setup-call-pipeline). Edit those values, not these. Keys must match the capitalized Type values in your Notion Calls DB.",
  "calendar_keywords": {
    "Sales": ["sales call", "discovery call", "intro call", "strategy call", "trial call"],
    "Coaching": ["coaching", "1:1", "private session", "check-in", "one-on-one"],
    "Group": ["group call", "office hours", "q&a", "implementation call"],
    "Workshop": ["workshop", "masterclass", "training"]
  },
  "client_names": []
}
